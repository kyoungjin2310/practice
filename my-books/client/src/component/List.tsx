import React from "react";

const List = () => {
  const click = () => {};

  return (
    <div>
      <h1>home</h1>
      <button onClick={click}>logout</button>
    </div>
  );
};

export default List;
